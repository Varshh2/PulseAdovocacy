<!-- softDeleteSubscribedUser.ejs -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Soft Delete Subscribed User</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: Arial, sans-serif;
            color: #000;
            margin: 0;
            padding: 0;
            min-height: 100vh;
            position: relative;
        }

        body::before {
            content: "";
            background: url('/bgimg.png') center center / cover fixed;
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            filter: blur(25px);
        }

        .navbar {
            background-color: #333;
            padding: 20px 0;
            color: #fff;
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 2;
        }

        .navbar-brand {
            color: #fff;
            font-weight: bold;
            font-size: 35px;
        }

        .highlight {
            color: #ff0000;
        }

        .navbar-nav {
            margin-left: auto;
        }

        .middle-container {
            position: absolute;
            top: 120px; /* Adjust based on navbar height */
            bottom: 60px; /* Adjust based on footer height */
            left: 50%;
            transform: translateX(-50%);
            width: 70%;
            background-color: rgba(255, 255, 255, 0.553);
            border-radius: 10px;
            padding: 20px;
        }

        .footer {
            background-color: #333;
            color: #fff;
            text-align: center;
            padding: 20px 0;
            position: absolute;
            bottom: 0;
            width: 100%;
            z-index: 2;
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg">
        <div class="container">
            <a class="navbar-brand" href="#">
                Pulse <span class="highlight">A</span>dvocacy
            </a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/dashboard">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/campaigns">Campaigns</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/legislativeBills">Legislative Bills</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Form for soft deleting subscribed user -->
<form action="/subscribedUsers/delete/<%= user._id %>?_method=DELETE" method="POST">
    <p>Are you sure you want to delete <%= user.firstName %> <%= user.lastName %>? This action cannot be undone.</p>
    <!-- Add hidden input fields if necessary -->
    <button type="submit" class="btn btn-danger">Yes, Delete</button>
    <a href="/subscribedUsers" class="btn btn-primary">Cancel</a>
</form>


    <!-- Footer -->
    <footer class="footer">
        &copy; 2024 Pulse Advocacy. All rights reserved.
    </footer>

    <!-- jQuery and Bootstrap JS -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
