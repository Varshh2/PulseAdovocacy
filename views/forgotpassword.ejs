<!-- forgotpassword.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #4a154b; /* Deep purple background */
            font-family: Arial, sans-serif; /* Font family */
            color: #333; /* Dark gray text color */
        }
        .logo {
            text-align: center;
            margin-top: 30px;
            font-family: 'Gothic', sans-serif; /* Gothic font */
            font-size: 24px; /* Adjust font size */
            color: #ffffff; /* White color */
            text-shadow: 2px 2px 4px #000000; /* Text shadow for effect */
        }
        .logo .highlight {
            color: #ff8000; /* Vibrant orange color for 'A' */
        }
        .forgot-password-container {
            background-color: rgba(255, 255, 255, 0.9); /* Slightly transparent white background */
            border-radius: 10px; /* Rounded corners */
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.5); /* Box shadow for a 3D effect */
            padding: 30px;
            max-width: 400px;
            margin: auto;
            margin-top: 50px; /* Adjust as needed */
        }
        .forgot-password-container h1 {
            text-align: center;
            margin-bottom: 30px;
            color: #333; /* Dark gray text color */
        }
        .forgot-password-container input[type="email"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #666; /* Medium gray border color */
            border-radius: 5px;
        }
        .forgot-password-container button[type="submit"] {
            width: 100%;
            padding: 10px;
            border: none;
            border-radius: 5px;
            background-color: #ff8000; /* Vibrant orange button color */
            color: #fff; /* White text color */
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s ease; /* Smooth transition on hover */
        }
        .forgot-password-container button[type="submit"]:hover {
            background-color: #e64d00; /* Darker orange color on hover */
        }
    </style>
</head>
<body>
    <div class="logo">
        <span>Pulse <span class="highlight">A</span>dvocacy</span>
    </div>
    <div class="forgot-password-container">
        <h1>Forgot Password</h1>
        <form action="/forgot-password" method="post">
            <input type="email" name="email" class="form-control" placeholder="Email" required><br>
            <button type="submit" class="btn btn-primary">Reset Password</button>
        </form>
    </div>
</body>
</html>
